#include <stdio.h>
/**
 * main - Entry point
 * Description - 'the program's description'
 * Return: always 0 (Success)
 */

int main(void)
{
	int C;

	for (C = 0; C < 10; ++C)
		printf("%d", C);
	     printf("\n");
	return (0);
}
